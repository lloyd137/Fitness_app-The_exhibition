<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessapp_theexhibition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/no_bear_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/no_bear_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ProgressActivity"></activity>
        <activity android:name=".activities.intro.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.creator.EditConfirmWorkoutActivity" />
        <activity android:name=".activities.creator.CreateExercisesListActivity" />
        <activity android:name=".activities.intro.IntroTextActivity" />
        <activity android:name=".activities.intro.IntroAnimationActivity" />
        <activity android:name=".activities.WorkoutFinishedActivity" />
        <activity android:name=".activities.WorkoutExecutionActivity" />
        <activity android:name=".activities.details.ExerciseDetailScreen" />
        <activity android:name=".activities.details.WorkoutDetailScreen" />
        <activity android:name=".activities.creator.CreateWorkoutActivity" />
        <activity android:name=".activities.ExercisesListActivity" />
        <activity android:name=".activities.WorkoutListActivity" />
        <activity android:name=".activities.MainMenuActivity" />
    </application>

</manifest>